spring:
  application:
    name: audit-service
  config:
    import: "optional:configserver:${SPRING_CONFIG_IMPORT_URI:http://localhost:8888}"
  cloud:
    config:
      fail-fast: false
      retry:
        max-attempts: 3
